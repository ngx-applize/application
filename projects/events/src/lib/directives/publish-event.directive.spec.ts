import { PublishEventDirective } from './publish-event.directive';

describe('PublishEventDirective', () => {
  it('should create an instance', () => {
    const directive = new PublishEventDirective();
    expect(directive).toBeTruthy();
  });
});
